#include <stdio.h>
#include <stdlib.h>
#include "TP_2.h"

int initEmployees (arrayEmployees[],ELEMENTS);
{
    if ( arrayEmployees[i] == -1)
    {
        printf ("No quedan lugares");
    }
    else{
          for (int i = 0; i < 1000; i ++)
    {
    printf ("Ingrese nombre de empleado: \n");
    gets (name);
    printf ("Ingrese apellido de empleado: \n");
    gets (lastName);  for (int i = 0; i < 1000; i ++)
    {
    printf ("Ingrese salario de empleado: \n");
    scanf ("%d", Employee[i].salary);
    printf ("Ingrese sector de empleado: \n");
    scanf ("%d", Employee[i].sector);
    }
         }

    int addEmployees (Employee[list], int len, int id, char name[],char lastName[],float salary,int sector)
    {
        printf ("Ingrese nombre de empleado nuevo: "\n);
        gets (name);
    }
    




}